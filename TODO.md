# TODO - Arreglo lista desplegable pestaña Hospitales

- [ ] Revisar en `index.html` cómo se construyen los `<select>` de `entidad` y `unidad`.
- [ ] Cambiar la lógica de `unidad` para que el `value` sea un identificador único (usar `clues`) en lugar de `nombre`.
- [ ] Actualizar `autoCompletarHospital()` para rellenar `municipio` y `clues` usando ese `clues` seleccionado.
- [ ] Verificar que al cambiar `entidad` se regenere correctamente la lista de `unidad` y se limpien campos.
- [ ] (Si aplica) mantener `cve` manual como está actualmente.

